const config = {
	database : {
		DATABASE : 'lightning-test-stellar',
		USERNAME : 'root',
		PASSWORD : 'root',
		PORT : 3306,
		HOST : '127.0.0.1',
	},
	// 启动端口
	port : 8000,
}

module.exports = config
